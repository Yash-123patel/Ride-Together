name: Scheduled Workflow
on:
  schedule:
    # This runs at 00:00 every day (UTC)
    - cron: '0 0 * * *'
  # Optional: allows manual triggering for testing
  workflow_dispatch: 

jobs:
  run-cron:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world
        run: echo "Executing scheduled task..."
